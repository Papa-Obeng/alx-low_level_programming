#include "main.h"

/**
 * mul - multiplier
 * @a - int to mul
 * @b - int to mul
 * result - the variable to hold the value after multiplication
 * Return: result
 */

int mul(int a, int b)
{
	return (a * b);
}
