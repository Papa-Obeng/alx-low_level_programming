/**
 * main-entry to the program that prints the strings using printf
 * return function evaluates to zero
 */
#include<stdio.h>

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return(0);
}
